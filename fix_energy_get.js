const fs = require('fs'); let content = fs.readFileSync('server/src/modules/daily/DailyContextOrchestrator.ts', 'utf8'); content = content.replace(/if \\(payload.interpretationVersion === 'v1' && payload.interpretation\\?\\.interpretationStatus === 'ready'\\) \\{\\s*return payload;\\s*\\}/, if (payload.interpretationVersion === 'v1') {\n                return payload;\n            }); fs.writeFileSync('server/src/modules/daily/DailyContextOrchestrator.ts', content); console.log('Done');
