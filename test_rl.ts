import { supabase } from './server/src/lib/supabase'; async function run() { const { data } = await supabase.auth.signInWithPassword({ email: 'test@example.com', password: 'password' }); const token = data.session?.access_token; if(!token) { console.log('no token'); return; } const res = await fetch('http://localhost:5173/api/lifeline/generate', { method: 'POST', headers: { 'Authorization': 'Bearer ' + token, 'Content-Type': 'application/json' }, body: JSON.stringify({ lang: 'es'}) }); console.log('Status:', res.status); console.log(await res.text()); } run();
