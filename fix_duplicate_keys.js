const fs = require('fs'); let content = fs.readFileSync('client/src/i18n/index.tsx', 'utf8'); let esKeys = ['personal_year', 'the_decade', 'main_metric', 'main_risk', 'virtue_to_develop', 'dormant_talent', 'energy_indicators', 'calculations_unavailable', 'evolution_axis_current_cycle', 'life_architecture']; esKeys.forEach(k => { const reg = new RegExp('(\\\\n\\\\s*' + k + '\\\\s*:.*?,)', 'g'); let matches = content.match(reg); if(matches && matches.length > 1) { content = content.replace(matches[1], ''); } }); fs.writeFileSync('client/src/i18n/index.tsx', content); console.log('Fixed duplicates');
